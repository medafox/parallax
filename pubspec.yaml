name: parallax
version: 1.0.3
homepage: https://github.com/medafox/parallax
description: A parallax package for touch-based devices.

environment:
  sdk: ">2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
